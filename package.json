{
  "name": "stream-chat-aggregator",
  "version": "0.1.0",
  "private": true,
  "description": "Unified multi-platform stream chat aggregator - Level 1 foundation",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
